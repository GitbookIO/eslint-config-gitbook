
// Named function
function sayHello(msg) {
    return 'Hello ' + msg;
}

// Anonymous function
const direBonjour = (msg) => {
    return 'Bonjour ' + msg;
};

module.exports = {
    sayHello,
    direBonjour
};
